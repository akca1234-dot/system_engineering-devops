#!/bin/bash
# This script shows the PID and command of all processes with "bash" in their name
for pid in $(ls /proc | grep '^[0-9]' | sort -n); do
    cmdline=$(cat /proc/"$pid"/comm 2>/dev/null)
    if echo "$cmdline" | grep -q "bash"; then
        echo "$pid $cmdline"
    fi
done
